import React from "react";
import './Desc.css'
const Description =()=>{
    return(
        <div className="descriptionbox">
<div className='descriptionbox-navigator'>
    <div className="descriptionbox-nav-box">
Description
</div>
<div className="descriptionbox-nav-box fade">
Reviews(122)
</div>
<div className="descriptionbox-description">
    <p>n that case, you might have a bit of misunderstanding. The purpose of React is to handle your DOM, not doing operations to database (like Eloquent ORM on Laravel, for instance)

What you could do is have your web server handle those operations, while providing a REST API endpoints for your React app to hit.</p>
<p>n that case, you might have a bit of misunderstanding. The purpose of React is to handle your DOM, not doing operations to database (like Eloquent ORM on Laravel, for instance)

What you could do is have your web server handle those operations, while providing a REST API endpoints for your React app to hit.</p>
</div>
    </div>

        </div>
    )
}
export default Description;